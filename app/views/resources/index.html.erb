index ya

<% @resources.each do |resource| %>
<%= resource.title %>
<%= link_to resource.url, "http://#{resource.url}", target: '_blank' %>
<%= button_to "delete", resource, :method=>:delete, :class=>:destroy %>

<% end %>

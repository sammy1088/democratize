index
<% @countries.each do |country| %>
<%= link_to country.title %>
<%= link_to 'Destroy', country_path(country),
                    method: :delete, data: { confirm: 'Are you sure?' } %>
	<% end %>